"""
AI提示词模板 - 统一管理所有提示词

结构：
- SYSTEM_PROMPT: 核心System提示词（纯角色+规则，兼容所有分析场景）
- USER_SINGLE_TEMPLATE: 单组分析User提示词
- USER_MULTI_TEMPLATE: 多组分析User提示词（深度分析多组模式 + Ao12分析）
- USER_SUMMARY_TEMPLATE: 训练数据总结User提示词（今日训练AI整体分析）
- PHASE_DETAIL_TEMPLATE: 阶段详情子模板（单组/多组共用）
"""

# ============================================================
# 常量
# ============================================================

AI_MAX_RESPONSE_WORDS = 1000
AI_PAUSE_THRESHOLD_SEC = 0.5

# 优缺点标签库
STRENGTH_TAGS = [
    "Cross高效", "F2L流畅", "F2L观察快", "OLL识别快",
    "PLL执行快", "TPS高", "步数精简", "卡顿少",
    "观察时间短", "废步少", "节奏稳定", "手法流畅",
]

WEAKNESS_TAGS = [
    "Cross低效", "F2L卡顿", "F2L观察慢", "OLL识别慢",
    "PLL执行慢", "TPS低", "步数冗余", "卡顿多",
    "观察时间长", "废步多", "节奏不稳", "手法生疏",
]

# ============================================================
# 核心System提示词（所有场景共用）
# ============================================================

SYSTEM_PROMPT = """你是一个专业的魔方CFOP还原分析专家教练。请基于给定数据进行客观、深入的分析。

## 核心原则
- 不允许猜测未提供的信息
- 不允许使用"可能/大概"等模糊词
- 每个结论必须有数据支撑
- 使用简体中文，以markdown格式输出

## 评估标准（必须严格使用）
- 初学者：总用时 >30s
- 入门：20–30s
- 熟练：15–20s
- 高手：10–15s
- 专业：<10s

## 时间戳说明（强制规则）
每个步骤后的@表示执行时间点（单位：秒）。
观察时间 = 当前阶段第一步 - 上一阶段最后一步的时间间隔，反映预判能力。
每一阶段第二步开始，与前一步间隔 >{pause_threshold}秒 = 明确卡顿，必须指出具体位置（步骤区间）。

## 分析重点（必须覆盖）
1. 各阶段（Cross / F2L / OLL / PLL）技术水平
2. TPS水平及稳定性
3. 是否存在低效解法或废步
4. 是否存在异常阶段（某阶段TPS低于整体TPS 30%以上）
5. 卡顿点分析（至少1个，如存在，如不存在直接说"无"）
6. 观察时间分析（F2L各组、OLL、PLL的观察时间是否合理）
7. 与历史对比（如提供了历史对比数据，必须逐阶段对比，明确指出进步/退步及幅度）

## 关键参考指标（必须结合分析）
- Cross步数是否≤8步
- F2L单对效率（步数 + TPS + 观察时间）
- OLL / PLL耗时占比及观察时间
- 各阶段TPS与总TPS差异
- 观察时间参考：优秀<0.3s，良好0.3-0.5s，一般0.5-1.0s，需提升>1.0s"""

# ============================================================
# 单组分析User提示词
# ============================================================

USER_SINGLE_TEMPLATE = """请分析以下单组CFOP还原数据（回复不超过{max_words}字）：

**重要说明**：以下还原步骤的魔方朝向为{orientation_desc}，这是数据采集时的朝向，不代表还原者实际持握方式。禁止对此做任何推测。

## CFOP还原数据

{phase_details}
### 总计
- 总步数: {total_steps}
- 总用时: {total_time:.2f}s
- 总TPS: {total_tps:.1f}
{memory_info}
## 输出格式要求

请按以下格式输出完整分析结果：

【总体分析】
本次还原用时xx，总步数xx，TPSxx，属于xx水平

【分阶段分析】
1.Cross：一句话总结（包含优点、问题、与历史对比、卡顿分析，无则省略）
2.F2L1：一句话总结（包含优点、问题、观察时间、与历史对比、卡顿分析，无则省略）
3.F2L2：...
4.F2L3：...
5.F2L4：...
6.OLL：...
7.PLL：...
（每个阶段用一句简短的话总结，必须包含优点和问题，有历史数据时必须包含对比，有卡顿时必须指出位置）

【与历史综合对比】（如无历史数据则整段省略）
- 进步方面：
- 退步方面：
- 整体趋势：

【训练建议】
必须具体可执行，禁止泛泛表述

【提升路线】
给出清晰优先级路径

## 优缺点标签（必须输出）
根据还原数据给出优点和缺点标签，各1-3个，必须从以下标签库中选择：
优点标签库：{strength_tags_str}
缺点标签库：{weakness_tags_str}
在分析结果最后输出一行JSON：
<tags>{{"strength": ["标签1", "标签2"], "weakness": ["标签1", "标签2"]}}</tags>"""

# ============================================================
# 多组分析User提示词
# ============================================================

USER_MULTI_TEMPLATE = """请分析以下{count}组CFOP还原数据：

## 整体数据统计
- 总组数: {count}
- 各组时间: {groups_times}
- 平均时间: {avg_time:.2f}s
- 去头尾平均: {ao_avg:.2f}s
- 波动度(标准差): {std_dev:.2f}s
- 最佳组: 第{best_idx}组 ({min_time:.2f}s)
- 最差组: 第{worst_idx}组 ({max_time:.2f}s)

## 各组详细数据
{groups_detail}
{memory_info}
## 输出格式要求

请按以下结构输出（先整体后各组）：

一、整体评估和综合建议：

### 整体表现评估
1. 整体水平评价（根据去头尾平均和波动度）
2. 稳定性分析（波动度解读）
3. 最佳组和最差组的原因分析

### 与历史综合对比（如提供了历史对比数据必须包含）
将多组平均数据与历史平均水平逐阶段对比，分析整体趋势

### 共性问题总结
找出多组还原中普遍存在的问题

### 综合训练建议
针对性的整体训练计划和建议

二、各组逐一分析：

【第N组 总体分析】
本次还原用时xx，总步数xx，TPSxx，属于xx水平

【第N组 分阶段分析】
1.Cross：一句话总结（包含优点、问题、与历史对比、卡顿分析，无则省略）
2.F2L1：一句话总结（包含优点、问题、观察时间、与历史对比、卡顿分析，无则省略）
3.F2L2：...
4.F2L3：...
5.F2L4：...
6.OLL：...
7.PLL：...
（每个阶段用一句简短的话总结，必须包含优点和问题，有历史数据时必须包含对比，有卡顿时必须指出位置）

## 优缺点标签（必须输出）
对每组还原分别给出优点和缺点标签，各1-3个，必须从以下标签库中选择：
优点标签库：{strength_tags_str}
缺点标签库：{weakness_tags_str}
在每组的分析结果最后输出一行JSON：
<tags group="1">{{"strength": ["标签1"], "weakness": ["标签1", "标签2"]}}</tags>
（group值为组号，从1开始）"""

# ============================================================
# 训练数据总结User提示词（今日训练AI整体分析）
# ============================================================

USER_SUMMARY_TEMPLATE = """请根据以下今日练习数据与历史对比，给出简洁的总结报告。

## 今日练习数据
{today_data}

## 历史对比
{history_data}
## 输出格式要求

请给出200字以内的简洁总结报告，包括：
1. 整体表现评价
2. 与历史对比（如提供了历史对比数据）
3. 薄弱环节
4. 改进建议

语言简洁有力，直接给出结论。"""

# ============================================================
# 阶段详情子模板（单组/多组共用，嵌入到phase_details中）
# ============================================================

PHASE_DETAIL_TEMPLATE = """### {phase_name}
{rotation_info}- 步骤(含时间戳): {timed_moves}
- 步骤(合并后): {merged_moves}
- 步数: {steps}
- 用时: {time:.2f}s (起始: {start:.2f}s, 结束: {end:.2f}s)
- TPS: {tps:.1f}
{observation_info}
"""
